server:
  port: 8090

spring:
  application:
    name: his-job-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        service: his-job-service
      config:
        server-addr: localhost:8848
        file-extension: yaml
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 1.0

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
